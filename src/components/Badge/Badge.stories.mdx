import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { withKnobs, text, color as colorKnob } from '@storybook/addon-knobs'

import Badge from './Badge'

# Badges

<Meta
  title='Components/Badge'
  decorators={[withKnobs]}
  component={Badge}
/>

## Default

<Preview>
  <Story name='Default'>{{
    components: { Badge },
    props: {
      text: {
        default: text('Badge text', 'Badge'),
      },
    },
    template: `
        <Badge>
            {{ text }}
        </Badge>
    `,
  }}</Story>
</Preview>

## Color

<Preview>
  <Story name='Color'>{{
    components: { Badge },
    props: {
      badgeColor: {
        default: colorKnob('Badge color', '#773333'),
      },
    },
    template: `
        <Badge :color="badgeColor">
            Badge
        </Badge>
    `,
  }}</Story>
</Preview>

## Props

<Props of={Badge} />
