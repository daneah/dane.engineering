import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'

import Card from './Card'

# Cards

<Meta
  title='Components/Card'
  component={Card}
/>

export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { Card },
    template: `<div style="width: 250px"><Card>
      <template #image><span v-html="image"></span></template>
      <template #title>{{ title }}</template>
      <template #description>{{ description }}</template>
    </Card></div>`,
})

<Canvas>
  <Story
    name='Canvas'
    args={{
      title: 'Kittens',
      description: 'They are adorable.',
      image: '<img src="https://placekitten.com/500/350" alt="kitty" />',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Canvas" />
