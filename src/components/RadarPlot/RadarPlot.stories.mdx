import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'

import RadarPlot from './RadarPlot'

# Radar plot

<Meta
  title='Components/RadarPlot'
  component={RadarPlot}
  argTypes={{
    levels: { control: { type: 'number', min: 2 } },
    characteristics: { control: 'object' },
    color: { control: 'color' }
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RadarPlot },
  template: '<div style="width: 350px"><RadarPlot :levels="levels" :characteristics="characteristics" :color="color" /></div>',
})

<Canvas>
  <Story
    name='Canvas'
    args={{
      levels: 5,
      characteristics: [
          { name: 'Flying', score: 1 },
          { name: 'Cooking', score: 3 },
          { name: 'Running', score: 0.5 },
          { name: 'Reading', score: 3 },
          { name: 'Swimming', score: 1.5 }
      ],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story='Canvas' />
